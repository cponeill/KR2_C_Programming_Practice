#include <stdio.h>

/* These are just quick code examples from the chapter. */

/* Character Pointers and Functions. */

char *pmessage; /* The pointer is declared. */
pmessage = "now is the time." /* assigns to 'pmessage' a pointer to the char array. */


char amessage[] = "now is the time";  /* This is an array. */
char *bmessage = "now is the time";   /* This is a pointer. */
