#include <stdio.h>

void strcpy(char *s, char *t)
{
    while(*s++ = *t++);
}

void strcat(char *s, char *t)
{
    while(*s)
    {
        ++s;
    }
    strcpy(s, t);
}

int main(void)
{
    /* Fill in code. */
}

